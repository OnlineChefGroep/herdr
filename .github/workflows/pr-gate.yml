name: PR Gate

on:
  pull_request_target:
    types: [opened, reopened]

jobs:
  check-contributor:
    uses: OnlineChefGroep/.github/.github/workflows/pr-gate-reusable.yml@main
    permissions:
      contents: read
      issues: write
      pull-requests: write
      members: read
